version: '2'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: pewpew
    volumes:
      - data:/var/lib/postgresql/data
volumes:
  data:
